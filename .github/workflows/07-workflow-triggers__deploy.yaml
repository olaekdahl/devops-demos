# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/07-workflow-triggers/.github/workflows/deploy.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[07-workflow-triggers] Manual deploy'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - run: 'echo "Deploying version=${{ github.event.inputs.version }}"

        echo "to environment=${{ github.event.inputs.environment }}"

        '
'on':
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        default: dev
        type: choice
        options:
        - dev
        - staging
        - prod
      version:
        description: Image tag to deploy
        required: true
        default: latest
