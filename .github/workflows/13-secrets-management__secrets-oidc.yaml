# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/13-secrets-management/.github/workflows/secrets-oidc.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: "[13-secrets-management] Secrets \u2014 OIDC (no long-lived keys)"
permissions:
  id-token: write
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: arn:aws:iam::<ACCOUNT_ID>:role/gha-deploy
        aws-region: us-west-2
    - run: aws sts get-caller-identity
    - run: aws s3 ls
defaults:
  run:
    working-directory: demos/13-secrets-management
'on':
  workflow_dispatch: {}
