# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/07-workflow-triggers/.github/workflows/pr.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[07-workflow-triggers] PR validation'
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: |
        echo "Validating PR #${{ github.event.pull_request.number }}"
defaults:
  run:
    working-directory: demos/07-workflow-triggers
'on':
  workflow_dispatch: {}
  pull_request:
    types:
    - opened
    - synchronize
    - reopened
    paths:
    - demos/07-workflow-triggers/**
    - .github/workflows/07-workflow-triggers__*
