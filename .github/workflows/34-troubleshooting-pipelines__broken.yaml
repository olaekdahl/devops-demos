# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/34-troubleshooting-pipelines/.github/workflows/broken.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: "[34-troubleshooting-pipelines] Broken \u2014 debug me"
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v55
      with:
        python-version: '3.12'
    - name: Print stuff
      run: 'cat <<EOF

        hello $USER from runner

        EOF

        '
    - name: Maybe broken
      run: pytest tests/
    - name: Leaky
      env:
        MY_TOKEN: ${{ secrets.MY_TOKEN }}
      run: 'set -x

        echo "Token=$MY_TOKEN"

        '
    - id: ver
      run: echo "version=1.2.3"
    - run: echo "got ${{ steps.ver.outputs.version }}"
defaults:
  run:
    working-directory: demos/34-troubleshooting-pipelines
'on':
  workflow_dispatch: {}
