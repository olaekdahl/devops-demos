# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/33-production-pipeline-concepts/.github/workflows/prod.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[33-production-pipeline-concepts] Production-grade pipeline'
permissions:
  contents: read
  packages: write
  id-token: write
env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/devops-app
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: pip
    - name: Lint
      run: pip install flake8 && flake8 app.py --max-line-length=120
    - name: Unit tests
      run: |
        pip install -r requirements.txt pytest httpx
        PYTHONPATH=$(pwd) pytest -v tests/
    - name: SAST (semgrep)
      uses: returntocorp/semgrep-action@v1
      with:
        config: p/python
    - name: "SCA \u2014 dependency vulns"
      run: pip install pip-audit && pip-audit -r requirements.txt
  build:
    needs: quality
    runs-on: ubuntu-latest
    outputs:
      digest: ${{ steps.push.outputs.digest }}
    steps:
    - uses: actions/checkout@v4
    - uses: docker/setup-buildx-action@v3
    - uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - id: push
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: ${{ env.IMAGE }}:${{ github.sha }}
        provenance: true
        sbom: true
    - name: Container scan (block on HIGH/CRITICAL)
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.IMAGE }}@${{ steps.push.outputs.digest }}
        severity: HIGH,CRITICAL
        exit-code: '1'
        ignore-unfixed: true
    - name: Generate SBOM
      uses: anchore/sbom-action@v0
      with:
        image: ${{ env.IMAGE }}@${{ steps.push.outputs.digest }}
        format: spdx-json
    - name: Cosign keyless sign
      uses: sigstore/cosign-installer@v3
    - run: cosign sign --yes ${{ env.IMAGE }}@${{ steps.push.outputs.digest }}
  policy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install conftest
      run: |
        curl -sSLo conftest.tar.gz https://github.com/open-policy-agent/conftest/releases/download/v0.55.0/conftest_0.55.0_Linux_x86_64.tar.gz
        tar -xzf conftest.tar.gz && sudo mv conftest /usr/local/bin/
    - name: Validate manifests
      run: conftest test deployment.yaml -p policies/
  deploy-canary:
    needs:
    - build
    - policy
    runs-on: ubuntu-latest
    environment: production
    steps:
    - run: "echo \"Deploy 10% traffic to image @ ${{ needs.build.outputs.digest }}\"\n# In real life:\
        \ Argo Rollouts / Flagger AnalysisTemplate watches SLOs.\necho \"Canary running for 5 minutes;\
        \ SLO probes succeed \u2192 promote.\"\n"
  promote:
    needs: deploy-canary
    runs-on: ubuntu-latest
    steps:
    - run: "echo \"Shifting to 100% \u2014 promotion complete\""
  rollback-on-failure:
    if: failure()
    needs:
    - deploy-canary
    - promote
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Auto-rollback to previous ReplicaSet"
        # kubectl rollout undo deployment/devops-app
defaults:
  run:
    working-directory: demos/33-production-pipeline-concepts
'on':
  workflow_dispatch: {}
  pull_request:
    paths:
    - demos/33-production-pipeline-concepts/**
    - .github/workflows/33-production-pipeline-concepts__*
