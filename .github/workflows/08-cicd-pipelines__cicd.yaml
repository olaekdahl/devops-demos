# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/08-cicd-pipelines/.github/workflows/cicd.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[08-cicd-pipelines] CI/CD Pipeline'
permissions:
  contents: read
concurrency:
  group: cicd-${{ github.ref }}
  cancel-in-progress: true
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: pip
    - run: pip install flake8
    - run: flake8 app.py --max-line-length=120
  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: pip
    - run: pip install -r requirements.txt pytest httpx
    - run: PYTHONPATH=$(pwd) pytest -v tests/
  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - run: pip install build && python -m build --wheel
    - name: Upload wheel artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-wheel
        path: dist/*.whl
        retention-days: 7
  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
    - uses: actions/download-artifact@v4
      with:
        name: app-wheel
        path: dist
    - run: |
        ls -lh dist/
        echo "Pretending to upload $(ls dist/) to S3 / Beanstalk / EKS..."
        # In a real pipeline this is `aws s3 cp` / `eb deploy` / `kubectl apply`.
  smoke:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
    - run: |
        echo "curl https://devops-demo.example.com/health"
        echo '{"status":"OK"}'           # pretend response
        echo "::notice::smoke test passed"
defaults:
  run:
    working-directory: demos/08-cicd-pipelines
'on':
  workflow_dispatch: {}
  pull_request:
    paths:
    - demos/08-cicd-pipelines/**
    - .github/workflows/08-cicd-pipelines__*
