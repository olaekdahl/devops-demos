# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/10-matrix-builds/.github/workflows/matrix.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[10-matrix-builds] Matrix tests'
jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        os:
        - ubuntu-latest
        - macos-latest
        python-version:
        - '3.11'
        - '3.12'
        - '3.13'
        exclude:
        - os: macos-latest
          python-version: '3.11'
        include:
        - os: ubuntu-latest
          python-version: '3.12'
          extra: with-coverage
    name: ${{ matrix.os }} / py${{ matrix.python-version }}${{ matrix.extra && format(' / {0}', matrix.extra)
      || '' }}
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: pip
    - run: pip install -r requirements.txt pytest httpx pytest-cov
    - name: Run tests
      run: PYTHONPATH=$(pwd) pytest -v tests/
    - name: Coverage report (special cell only)
      if: matrix.extra == 'with-coverage'
      run: PYTHONPATH=$(pwd) pytest --cov=app --cov-report=term-missing tests/
defaults:
  run:
    working-directory: demos/10-matrix-builds
'on':
  workflow_dispatch: {}
