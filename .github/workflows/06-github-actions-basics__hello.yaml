# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/06-github-actions-basics/.github/workflows/hello.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[06-github-actions-basics] Hello Actions'
jobs:
  greet:
    name: Greet the runner
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Print runner info
      run: 'echo "Repo:   ${{ github.repository }}"

        echo "SHA:    ${{ github.sha }}"

        echo "Actor:  ${{ github.actor }}"

        echo "Event:  ${{ github.event_name }}"

        uname -a

        '
    - name: Use Python action
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Show Python version
      run: python --version
defaults:
  run:
    working-directory: demos/06-github-actions-basics
'on':
  workflow_dispatch: {}
