# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/11-parallel-jobs/.github/workflows/parallel.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[11-parallel-jobs] Parallel CI'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: pip
    - run: pip install flake8
    - run: flake8 app.py --max-line-length=120
  unit-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: pip
    - run: pip install -r requirements.txt pytest httpx
    - run: PYTHONPATH=$(pwd) pytest -v tests/
  security:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - run: pip install pip-audit bandit
    - name: Audit dependencies
      run: pip-audit -r requirements.txt || true
    - name: Static analysis
      run: bandit -r . -x ./tests || true
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - run: pip install build && python -m build --wheel
    - uses: actions/upload-artifact@v4
      with:
        name: app-wheel
        path: dist/*.whl
defaults:
  run:
    working-directory: demos/11-parallel-jobs
'on':
  workflow_dispatch: {}
  pull_request:
    paths:
    - demos/11-parallel-jobs/**
    - .github/workflows/11-parallel-jobs__*
