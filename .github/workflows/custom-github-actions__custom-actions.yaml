# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/custom-github-actions/.github/workflows/custom-actions.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[custom-github-actions] Custom Actions Demo'
jobs:
  composite:
    name: Composite action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - id: greet
      uses: ./demos/custom-github-actions/actions/greet-composite
      with:
        who: ${{ inputs.who || 'Ada' }}
    - name: Print greeting
      run: echo "greeting=${{ steps.greet.outputs.greeting }}"
  javascript:
    name: JavaScript action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - id: greet
      uses: ./demos/custom-github-actions/actions/greet-javascript
      with:
        who: ${{ inputs.who || 'Ada' }}
    - name: Print greeting
      run: echo "greeting=${{ steps.greet.outputs.greeting }}"
  docker:
    name: Docker action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - id: greet
      uses: ./demos/custom-github-actions/actions/greet-docker
      with:
        who: ${{ inputs.who || 'Ada' }}
    - name: Print greeting
      run: echo "greeting=${{ steps.greet.outputs.greeting }}"
defaults:
  run:
    working-directory: demos/custom-github-actions
'on':
  workflow_dispatch:
    inputs:
      who:
        description: Name to greet
        required: false
        default: Ada
