# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/13-secrets-management/.github/workflows/secrets-basic.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: "[13-secrets-management] Secrets \u2014 basic"
jobs:
  use-secrets:
    runs-on: ubuntu-latest
    steps:
    - name: Demonstrate masking
      run: echo "Token is ${{ secrets.MY_API_TOKEN }}"
    - name: NEVER do this  (decoded form bypasses masking)
      run: "# The following is intentionally bad to teach the failure mode:\n# echo \"${{ secrets.MY_API_TOKEN\
        \ }}\" | base64\n# The base64 is NOT in the masking dictionary \u2014 it would leak.\necho \"\
        (skipped \u2014 illustrative only)\"\n"
    - name: Configure AWS CLI from secrets
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
        aws-region: us-west-2
    - name: Verify caller identity
      run: aws sts get-caller-identity
defaults:
  run:
    working-directory: demos/13-secrets-management
'on':
  workflow_dispatch: {}
