# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/09-unit-testing/.github/workflows/tests.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[09-unit-testing] Unit tests'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: pip
    - run: pip install -r requirements.txt pytest httpx pytest-cov
    - name: Run tests with coverage
      run: 'PYTHONPATH=$(pwd) pytest -v --cov=app --cov-report=term-missing tests/

        '
    - name: Fail if coverage < 80%
      run: 'PYTHONPATH=$(pwd) pytest --cov=app --cov-fail-under=80 tests/

        '
defaults:
  run:
    working-directory: demos/09-unit-testing
'on':
  workflow_dispatch: {}
  pull_request: null
