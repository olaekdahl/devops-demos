# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/07-workflow-triggers/.github/workflows/issue.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[07-workflow-triggers] New-issue logger'
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "New issue: ${{ github.event.issue.title }} by ${{ github.event.issue.user.login }}"
'on':
  workflow_dispatch: {}
