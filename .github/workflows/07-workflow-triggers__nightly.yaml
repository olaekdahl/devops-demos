# GENERATED by tools/deploy_demo_workflows.py — DO NOT EDIT.
# Source: demos/07-workflow-triggers/.github/workflows/nightly.yaml
# Regenerate with: python3 tools/deploy_demo_workflows.py --clean --mode=dispatch
name: '[07-workflow-triggers] Nightly security scan'
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Pretend to scan
      run: echo "pip-audit / trivy / bandit would run here"
defaults:
  run:
    working-directory: demos/07-workflow-triggers
'on':
  workflow_dispatch: {}
