#!/usr/bin/env bash
# Extracted commands from 33-production-pipeline-concepts.md
# REVIEW BEFORE RUNNING — some commands are interactive or destructive.
# Run blocks individually rather than the whole script if unsure.
set -euo pipefail

# git add . && git commit -m "ci: production pipeline" && git push  # parent-repo op — review & run manually