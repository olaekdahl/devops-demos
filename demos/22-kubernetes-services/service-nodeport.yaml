apiVersion: v1
kind: Service
metadata:
  name: devops-app-np
spec:
  type: NodePort
  selector: { app: devops-app }
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080                # 30000-32767 range; host port on every node
